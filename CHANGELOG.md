# Changelog

See [Releases](https://github.com/jedwards1230/claude-plugin-actions/releases) for the authoritative changelog.

Versioning policy:

- Specific releases (`v0.1.2`) are immutable.
- Moving tags `v0` and `v0.1` are fast-forwarded by the release workflow to the latest matching release.
- See the [README's Version pinning section](README.md#version-pinning) for which ref to depend on.
